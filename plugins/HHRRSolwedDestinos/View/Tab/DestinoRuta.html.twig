{% extends "Master/MenuTemplate.html.twig" %}

{% block body %}
<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-header">
                    <h3 class="card-title">
                        <i class="fas fa-map-marker-alt fa-fw" aria-hidden="true"></i>
                        {{ i18n.trans('destinos') }}
                    </h3>
                </div>
                <div class="card-body">
                    <div class="alert alert-info">
                        <h4><i class="fas fa-info-circle"></i> {{ i18n.trans('coming-soon') }}</h4>
                        <p>{{ i18n.trans('destinos-management-will-be-available-soon') }}</p>
                        <p><strong>{{ i18n.trans('ruta-id') }}:</strong> {{ fsc.getViewModelValue(fsc.getMainViewName(), 'id') }}</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}